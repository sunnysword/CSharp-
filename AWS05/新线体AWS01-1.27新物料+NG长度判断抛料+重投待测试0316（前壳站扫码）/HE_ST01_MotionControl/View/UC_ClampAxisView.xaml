<UserControl x:Class="DS_SorterAndOven.View.MainUnits.UC_ClampAxisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DS_SorterAndOven.View.MainUnits"
             mc:Ignorable="d" 
              d:DesignHeight="450" d:DesignWidth="800" Loaded="UserControl_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>

        </Grid.ColumnDefinitions>

        <ContentControl x:Name="cc_ClampDebug" Grid.ColumnSpan="2"></ContentControl>

        <GroupBox Grid.Row="1" Header="旋转轴位置设置(单位：角度)">
            <ItemsControl x:Name="itemsControlRotatedPos" Margin="10" FontSize="15" Grid.IsSharedSizeScope="True">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="sg_title"></ColumnDefinition>
                                <ColumnDefinition Width="Auto" ></ColumnDefinition>
                                <ColumnDefinition Width="Auto"></ColumnDefinition>

                                <ColumnDefinition></ColumnDefinition>

                            </Grid.ColumnDefinitions>

                            <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center" Text="{Binding Name}" Margin="0,0,5,0"></TextBlock>
                            <TextBox VerticalAlignment="Center" Text="{Binding PosMsg.GetValue}" Width="80" Padding="2" VerticalContentAlignment="Center" HorizontalContentAlignment="Left" Grid.Column="1" Margin="0,0,5,0"></TextBox>

                            <Button VerticalAlignment="Center" Tag="{Binding}" Content="Mov" Grid.Column="2" Padding="10,5" Click="btnClampRotatetdMovPos_Click"></Button>

                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="{x:Type ContentPresenter}">
                        <Style.Setters>
                            <Setter Property="Margin" Value="5"></Setter>
                        </Style.Setters>
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </GroupBox>
        <Grid Grid.Column="1" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>

                <RowDefinition></RowDefinition>

            </Grid.RowDefinitions>
            <Button Content="Save" FontSize="20" Padding="10,5" HorizontalAlignment="Right" Margin="5" Click="btnSaveClick"></Button>

            <StackPanel Grid.Row="1" Orientation="Horizontal">
                <TextBlock Text="旋转速度(0-100)" FontSize="15"></TextBlock>
                <TextBox x:Name="tb_RotatedSpeed" Width="80" FontSize="15"></TextBox>
            </StackPanel>

        </Grid>
    </Grid>
</UserControl>
