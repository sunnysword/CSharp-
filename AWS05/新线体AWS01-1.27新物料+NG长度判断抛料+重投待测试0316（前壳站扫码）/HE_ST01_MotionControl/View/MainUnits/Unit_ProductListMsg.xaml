<UserControl x:Class="Handler.View.MainUnits.Unit_ProductListMsg"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Handler.View.MainUnits"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>

        </Grid.RowDefinitions>
        <GroupBox Grid.Row="0" Header="Products List" FontSize="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>
                <DataGrid Name="Dg_Msg" MaxHeight="400" IsReadOnly="True"  Margin="1"  AutoGenerateColumns="False" RowHeight="15" >
                    <DataGrid.Columns>
                        <DataGridTextColumn  Header="SN"  Width="170" CanUserSort="False"  Binding="{Binding SN}"  IsReadOnly="True" >
                        </DataGridTextColumn>

                        <DataGridTemplateColumn Header="目前结果" Width="60"  CanUserSort="False"  >
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Result }" Foreground="Black"  Width="100"  HorizontalAlignment="Left"  />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="当前站位" Width="130" CanUserSort="False"  Binding="{Binding StationName}" IsReadOnly="True" >
                        </DataGridTextColumn>
                       
                        
                        <DataGridTextColumn Header="错误信息" Width="100" CanUserSort="False"  Binding="{Binding FailedMsg}"  IsReadOnly="True" >
                        </DataGridTextColumn>
                        <!--<DataGridTextColumn Header="RFID" Width="40" CanUserSort="False"  Binding="{Binding RFID}"  IsReadOnly="True" >
                        </DataGridTextColumn>-->
                        <!--<DataGridTextColumn Header="Operator" Width="100" CanUserSort="False"  Binding="{Binding Operator}"  IsReadOnly="True" >
                        </DataGridTextColumn>-->

                        






                    </DataGrid.Columns>


                </DataGrid>
                <!--<Button Name="btn_Clear" Cursor="Hand" Content="Clear" Grid.Row="1" Height="20" Width="40" VerticalAlignment="Center" HorizontalAlignment="Left" Click="Btn_Clear_Click"></Button>-->

            </Grid>
        </GroupBox>


    </Grid>
</UserControl>
